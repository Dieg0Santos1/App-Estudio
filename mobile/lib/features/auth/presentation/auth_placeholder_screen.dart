import '../../../shared/presentation/placeholder_screen.dart';

class AuthPlaceholderScreen extends PlaceholderScreen {
  const AuthPlaceholderScreen({super.key})
    : super(
        title: 'Autenticacion',
        description: 'Login, registro y recuperacion de cuenta.',
      );
}

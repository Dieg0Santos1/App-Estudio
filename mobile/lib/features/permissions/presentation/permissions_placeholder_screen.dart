import '../../../shared/presentation/placeholder_screen.dart';

class PermissionsPlaceholderScreen extends PlaceholderScreen {
  const PermissionsPlaceholderScreen({super.key})
    : super(
        title: 'Permisos',
        description:
            'Permisos Android para enfoque, archivos y notificaciones.',
      );
}

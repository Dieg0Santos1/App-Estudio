import '../../../shared/presentation/placeholder_screen.dart';

class ProfilePlaceholderScreen extends PlaceholderScreen {
  const ProfilePlaceholderScreen({super.key})
    : super(
        title: 'Perfil',
        description: 'Preferencias, modo de bloqueo y configuracion.',
      );
}
